## API Report File for "@betteroffice/docx"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ColorValue } from '@betteroffice/drawingml';
import { Node as Node_2 } from 'prosemirror-model';
import { PresetGeometryPathCommand } from '@betteroffice/drawingml';
import { ShapeFill } from '@betteroffice/drawingml';
import { ShapeOutline } from '@betteroffice/drawingml';
import { ShapeTextBody as ShapeTextBody_2 } from '@betteroffice/drawingml';
import { ShapeType } from '@betteroffice/drawingml';
import { Theme } from '@betteroffice/drawingml';
import { ThemeColorSlot } from '@betteroffice/drawingml';

// @public
export function applyFootnotePresentation(blocks: LayoutBlock[], displayNumber: number, displayLabel?: string): LayoutBlock[];

// @public
export function buildFootnoteContentMap(footnotes: Footnote[], footnoteRefs: Array<{
    footnoteId: number;
}>, contentWidth: number | ((ref: {
    footnoteId: number;
}) => number), options: ConvertFootnoteOptions): Map<number, FootnoteContent>;

// @public
export function buildFootnoteRenderItems(pageFootnoteMap: Map<number, number[]>, footnoteContentMap: Map<number, FootnoteContent>, doc: Document_2 | null): Map<number, FootnoteRenderItem[]>;

// @public
export interface BundledFontProvider {
    resolve(family: string, bold: boolean, italic: boolean): (() => Promise<ArrayBuffer>) | undefined;
    resolveLastResort?(family: string, bold: boolean, italic: boolean): (() => Promise<ArrayBuffer>) | undefined;
    resolveScriptFallback?(script: FontScript, bold: boolean, italic: boolean): (() => Promise<ArrayBuffer>) | undefined;
}

// @public
export function calculateFootnoteReservedHeights(pageFootnoteMap: Map<number, number[]>, footnoteContentMap: Map<number, {
    height: number;
    noteKind?: NoteKind;
}>, columns?: number | Map<number, number>): Map<number, number>;

// @public (undocumented)
export function calculateHeaderFooterVisualBounds(blocks: LayoutBlock[], measures: BlockExtent[], flowHeight: number, metrics: HeaderFooterMetrics): {
    visualTop: number;
    visualBottom: number;
};

// @public
type CaretPosition_2 = {
    x: number;
    y: number;
    height: number;
    pageIndex: number;
};
export { CaretPosition_2 as CaretPosition }

// @public
export function clampFloatingWrapMargins(leftMargin: number, rightMargin: number, contentWidth: number): {
    leftMargin: number;
    rightMargin: number;
};

// @public
export function collectFootnoteRefs(blocks: LayoutBlock[]): FootnoteRefLocation[];

// @public (undocumented)
export function columnWidthForSection(config: SectionLayoutConfig): number;

// @public
export function computeHfCaretRectsFromDisplayList(queries: DisplayListQueries, section: 'header' | 'footer', rId: string, pos: number, pageIndex?: number): DisplayListRect[];

// @public
export function computeHfSelectionRectsFromDisplayList(queries: DisplayListQueries, section: 'header' | 'footer', rId: string, from: number, to: number, pageIndex?: number): DisplayListRect[];

// @public
export function computePerBlockWidths(blocks: LayoutBlock[], initialConfig: SectionLayoutConfig, finalConfig: SectionLayoutConfig): number[];

// @public
export function contributesToHeaderFooterFlowHeight(block: LayoutBlock): boolean;

// @public
export function convertBorderSpecToLayout(border: {
    style?: string;
    size?: number;
    space?: number;
    color?: {
        rgb?: string;
        themeColor?: string;
        themeTint?: string;
        themeShade?: string;
    };
}, theme?: Theme | null): BorderStyle | undefined;

// @public
export type ConvertFootnoteOptions = {
    styles?: StyleDefinitions | null;
    theme?: Theme | null;
    yrsStoryBlocks?: (storyId: string) => LayoutBlock[] | null;
    measureBlocks: MeasureBlocksFn;
    defaultTabStopTwips?: number | null;
};

// @public
export function convertFootnoteToContent(footnote: Footnote | Endnote, displayNumber: number, contentWidth: number, options: ConvertFootnoteOptions, presentation?: {
    noteKind?: NoteKind;
    displayLabel?: string;
    anchor?: FootnoteContent['anchor'];
}): FootnoteContent;

// @public (undocumented)
export type ConvertHeaderFooterOptions = {
    styles?: StyleDefinitions | null;
    theme?: Theme | null;
    measureBlocks: MeasureBlocksFn;
    yrsStoryBlocks?: (storyId: string) => LayoutBlock[] | null;
    defaultTabStopTwips?: number | null;
};

// @public
export function convertHeaderFooterPmDocToContent(pmDoc: Node_2, contentWidth: number, metrics: HeaderFooterMetrics, options: ConvertHeaderFooterOptions, hfStoryId?: string): HeaderFooterContent | undefined;

// @public
export function convertHeaderFooterToContent(headerFooter: HeaderFooter | null | undefined, contentWidth: number, metrics: HeaderFooterMetrics, options: ConvertHeaderFooterOptions, hfStoryId?: string): HeaderFooterContent | undefined;

// @public
export function countTableColumns(tableBlock: TableBlock): number;

// @public
export function createRustMeasureSource(opts: {
    engine: RustTextEngine;
    bundled?: BundledFontProvider;
}): RustMeasureSource;

// @public
export const DEFAULT_BODY_MARGIN_PX = 96;

// @public
export const DEFAULT_HF_DISTANCE_PX = 48;

// @public
export const DEFAULT_PAGE_HEIGHT_PX = 1056;

// @public
export const DEFAULT_PAGE_WIDTH_PX = 816;

// @internal
export function demoteBlockLikeFloatingTables(_blocks: LayoutBlock[], _blockWidths: number[], _fallbackWidth: number): void;

// @public
export function distributeFootnotesIntoColumns<T extends {
    height: number;
}>(items: T[], columns: number): T[][];

// @public
export interface EmbeddedFaceInput {
    data: ArrayBuffer;
    family: string;
    style: 'normal' | 'italic';
    subsetted: boolean;
    weight: 'normal' | 'bold';
}

// @public
export function extendMarginsForHeaderFooter(input: ExtendMarginsForHeaderFooterInput): ExtendMarginsForHeaderFooterResult;

// @public (undocumented)
export interface ExtendMarginsForHeaderFooterInput {
    bodyBlocks?: LayoutBlock[];
    finalMargins: PageMargins;
    footers?: Array<HeaderFooterContent | undefined>;
    headers?: Array<HeaderFooterContent | undefined>;
    margins: PageMargins;
    // (undocumented)
    pageSize: {
        w: number;
        h: number;
    };
    warn?: (message: string) => void;
}

// @public (undocumented)
export interface ExtendMarginsForHeaderFooterResult {
    // (undocumented)
    finalMargins: PageMargins;
    // (undocumented)
    margins: PageMargins;
}

// @public (undocumented)
export interface FloatingExclusionRect {
    // (undocumented)
    distBottom: number;
    // (undocumented)
    distLeft: number;
    // (undocumented)
    distRight: number;
    // (undocumented)
    distTop: number;
    // (undocumented)
    height: number;
    side: 'left' | 'right';
    // (undocumented)
    width: number;
    // (undocumented)
    wrapText?: WrapTextDirection;
    // (undocumented)
    wrapType?: string;
    x: number;
    y: number;
}

// @public (undocumented)
export interface FloatingImageZone {
    // (undocumented)
    bottomY: number;
    fullWidthBlock?: boolean;
    // (undocumented)
    leftMargin: number;
    // (undocumented)
    rightMargin: number;
    // (undocumented)
    segments?: FloatingLineSegmentZone[];
    // (undocumented)
    topY: number;
}

// @public (undocumented)
export interface FloatingLineSegmentZone {
    // (undocumented)
    availableWidth: number;
    // (undocumented)
    leftOffset: number;
}

// @public
export type FloatPageGeometry = PageGeometry;

// @public
export type FontScript = 'cjk-sc' | 'cjk-tc' | 'cjk-jp' | 'cjk-kr' | 'arabic' | 'hebrew';

// @public
export interface FontStyle {
    // (undocumented)
    bold?: boolean;
    // (undocumented)
    fontFamily?: string;
    fontSize?: number;
    // (undocumented)
    italic?: boolean;
    letterSpacing?: number;
}

// @public
export const FOOTNOTE_COLUMN_GAP_PX = 24;

// @public
export const FOOTNOTE_SEPARATOR_HEIGHT = 12;

// @public
export type FootnoteRefLocation = {
    footnoteId: number;
    pmPos: number;
    noteKind?: NoteKind;
    tableBlockId?: BlockId;
    rowIndex?: number;
};

// @public
export function footnoteReservedHeightsEqual(a: Map<number, number>, b: Map<number, number>): boolean;

// @public
export function getColumns(sectionProps: SectionProperties | null | undefined): ColumnLayout | undefined;

// @public (undocumented)
export function getFloatingMargins(lineY: number, lineHeight: number, zones: FloatingImageZone[] | undefined, paragraphYOffset: number): FloatingLineMargins;

// @public
export function getMargins(sp: SectionProperties | null | undefined): PageMargins;

// @public
export function getPageSize(sp: SectionProperties | null | undefined): {
    w: number;
    h: number;
};

// @public
export function getRustTextEngine(): Promise<RustTextEngine>;

// @public
export function hashParagraphBlock(block: ParagraphBlock): string;

// @public
export type HeaderFooterMetrics = {
    section: 'header' | 'footer';
    pageSize: {
        w: number;
        h: number;
    };
    margins: PageMargins;
};

// @internal
export function isBlockLikeFloatingTable(block: TableBlock, contentWidth: number): boolean;

// @public
export function mapFootnotesToPages(pages: Page[], footnoteRefs: FootnoteRefLocation[]): Map<number, number[]>;

// @public
export const MAX_FOOTNOTE_LAYOUT_PASSES = 6;

// @public
export type MeasureBlockFn = (block: LayoutBlock, contentWidth: number, floatingZones?: FloatingImageZone[], cumulativeY?: number) => BlockExtent;

// @public
export type MeasureBlocksFn = (blocks: LayoutBlock[], contentWidth: number) => BlockExtent[];

// @public
export function measureBlocksWithFloats(blocks: LayoutBlock[], contentWidth: number | number[], measureBlock: MeasureBlockFn, pageGeometry?: FloatPageGeometry): BlockExtent[];

// @public
export function measureTableBlock(tableBlock: TableBlock, contentWidth: number, measureBlock: (block: LayoutBlock, contentWidth: number) => BlockExtent): TableExtent;

// @public
export function measureTableCellBlockVisualHeight(block: LayoutBlock, blockMeasure: BlockExtent): number;

// @public (undocumented)
export function normalizeHeaderFooterMeasureBlocks(blocks: LayoutBlock[]): LayoutBlock[];

// @public
export function normalizeTableColumnWidths(columnWidths: number[], colCount: number, targetWidth: number): number[];

// @public (undocumented)
export function rectsToFloatingZones(rects: FloatingExclusionRect[], contentWidth: number): FloatingImageZone[];

// @public (undocumented)
export function resetBlockIdCounter(): void;

// @internal
export function resolveCellGrid(tableBlock: TableBlock): ResolvedGridCell[];

// @public
export interface ResolvedGridCell {
    // (undocumented)
    cellIndex: number;
    // (undocumented)
    colSpan: number;
    // (undocumented)
    columnIndex: number;
    // (undocumented)
    rowIndex: number;
    // (undocumented)
    rowSpan: number;
}

// @public
export function resolveHeaderFooter(doc: Document_2 | null, sp: SectionProperties | null | undefined): {
    header: HeaderFooter | null;
    footer: HeaderFooter | null;
    firstHeader: HeaderFooter | null;
    firstFooter: HeaderFooter | null;
};

// @public (undocumented)
export function resolveHeaderFooterVisualTop(run: ImageRun, paragraphY: number, flowHeight: number, metrics: HeaderFooterMetrics): number;

// @public
export function resolveListTemplate(template: string, counters: number[], levelNumFmts: NumberFormat[] | undefined): string;

// @internal
export function resolveTableColumnWidths(tableBlock: TableBlock, contentWidth: number): number[];

// @internal
export function resolveTableTotalWidthPx(tableBlock: TableBlock, contentWidth: number): number;

// @internal
export function resolveTableWidthPx(value: number | undefined, widthType: string | undefined, parentWidth: number): number | undefined;

// @public
export interface RustMeasureSource {
    clear(): void;
    createMeasureBlock(): MeasureBlockFn;
    getDocumentFontChains(blocks: LayoutBlock[]): Record<string, number[]>;
    getStats(): RustMeasureStats;
    hasPendingFonts(): boolean;
    prepareFonts(blocks: LayoutBlock[]): Promise<boolean>;
    setCompat(flags: CompatibilityFlags | undefined): void;
    setEmbeddedFaces(faces: EmbeddedFaceInput[]): void;
    setScriptHints(hints: {
        eastAsia?: string;
    } | undefined): void;
}

// @public
export interface RustMeasureStats {
    rustMeasured: number;
    syntheticBlockKinds: Record<string, number>;
    syntheticFallback: number;
    syntheticFontUnready: number;
    syntheticUncovered: number;
}

// @public
export interface RustTextEngine {
    clearFonts(): void;
    measureParagraphJson(input: string): string;
    registerFont(bytes: Uint8Array): number;
}

// @public
export type SelectionRect = {
    x: number;
    y: number;
    width: number;
    height: number;
    pageIndex: number;
};

// @public
export function stabilizeFootnoteLayout(args: StabilizeFootnoteLayoutArgs): StabilizeFootnoteLayoutResult;

// @public (undocumented)
export interface StabilizeFootnoteLayoutArgs {
    // (undocumented)
    blocks: LayoutBlock[];
    footnoteColumns?: number | Map<number, number>;
    // (undocumented)
    footnoteContentMap: Map<number, FootnoteContent>;
    // (undocumented)
    footnoteRefs: FootnoteRefLocation[];
    initialLayout: Layout;
    // (undocumented)
    layoutOpts: LayoutOptions;
    mapReferencesToPages?: (pages: Page[], refs: FootnoteRefLocation[]) => Map<number, number[]>;
    // (undocumented)
    measures: BlockExtent[];
    paginate: (measured: MeasuredBlock[], options: LayoutOptions) => Layout;
}

// @public (undocumented)
export interface StabilizeFootnoteLayoutResult {
    converged: boolean;
    // (undocumented)
    layout: Layout;
    // (undocumented)
    pageFootnoteMap: Map<number, number[]>;
}

// @public
export function toLayoutBlocks(doc: Node_2, options?: ToLayoutBlocksOptions): LayoutBlock[];

// @public
export type ToLayoutBlocksOptions = {
    defaultFont?: string;
    defaultSize?: number;
    theme?: Theme | null;
    pageContentHeight?: number;
    defaultTabStopTwips?: number;
    listCounters?: Map<number, number[]>;
    listSeenNumIds?: Set<string>;
};

// @public
export function twipsToPixels(twips: number): number;

```
