---
"@betteroffice/pptx": patch
"@betteroffice/rust-crates": patch
---

Turn `eaVert` and `mongolianVert` text boxes the way `vert` turns, run Mongolian columns left to right, and stack `wordArtVert` and `wordArtVertRtl` one character to a line.
