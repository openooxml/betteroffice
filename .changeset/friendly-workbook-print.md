---
"@betteroffice/xlsx": minor
---

Add font-aware range printing with device-specific grid geometry and optional print gridlines. Paint cell and preview fonts at their declared point size in print and raster output, and scale on-screen canvas text by the same points-to-pixels factor, so default-zoom cell text grows by roughly a third and pixel-based consumer tests shift, with consistent text alignment across canvas and raster rendering.
