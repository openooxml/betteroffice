---
title: Packages
description: What to install from npm, crates.io, and PyPI, and what each package does
---

Every BetterOffice package is a face on the same Rust engines — one parser, one
editing core, and one layout pass per format. What changes between registries is
the runtime you call them from and how much of the pipeline is exposed.

| Runtime | Install | Guide |
| --- | --- | --- |
| Browser or React | `npm install @betteroffice/docx-react` | [JavaScript](./javascript) |
| Native Node.js/Bun | `npm install @betteroffice/docx-native` | [JavaScript](./javascript#run-natively-in-nodejs-and-bun) |
| Native Rust | `cargo add betteroffice-docx` | [Rust](./rust) |
| Python | `pip install betteroffice-docx` | [Python](./python) |

## What exists per format

| Format | npm | crates.io | PyPI |
| --- | --- | --- | --- |
| DOCX | `@betteroffice/docx`, `@betteroffice/docx-react`, `@betteroffice/docx-native` | `betteroffice-docx` | `betteroffice-docx` |
| XLSX | `@betteroffice/xlsx`, `@betteroffice/xlsx-react`, `@betteroffice/xlsx-native` | `betteroffice-xlsx` | `betteroffice-xlsx` |
| PPTX | `@betteroffice/pptx`, `@betteroffice/pptx-react`, `@betteroffice/pptx-native` | `betteroffice-pptx` | `betteroffice-pptx` |

All three registries carry DOCX, XLSX, and PPTX. The core, React, and locale
packages for each format are released together. Version numbers differ between
formats and registries; use the registry links for current releases.

[VSDX](./vsdx) is available from source with a browser demo. Its npm packages and
Rust crates are not yet published.

## Fonts

Word documents overwhelmingly reference the MS core fonts, whose binaries cannot
be redistributed. These packages ship the open metric-compatible replacements —
same advance widths, so line breaks and pagination match Word.

| Package | Size | Contents |
| --- | --- | --- |
| `@betteroffice/fonts` | 7.9 MB | Carlito, Caladea, Liberation Sans/Serif/Mono, Noto Hebrew/Arabic |
| `@betteroffice/fonts-cjk` | 33 MB | Noto Sans SC/TC/JP/KR, Noto Serif SC |

The engine loads no fonts on its own and never reaches for a font package.
Install `@betteroffice/fonts` and hand it over once, before any editor mounts:

```ts
import { configureDefaultFonts } from "@betteroffice/docx/layout";
import * as fonts from "@betteroffice/fonts";

configureDefaultFonts({ fonts });
```

Add a `baseUrl` alongside it to fetch the binaries from your own CDN instead of
the package assets; the module stays required either way, because it carries the
manifest that maps a Word font name to a face. Add `@betteroffice/fonts-cjk`
only for Chinese, Japanese, or Korean text — it is separate so nobody installs
33 MB they do not need.

Unconfigured, families a document does not embed measure with synthetic metrics
and pagination will not match Word. Neither package is installed automatically;
both are declared optional peers.

## Picking one

- **An editor UI in a React app** — the `-react` packages. They own the toolbar,
  canvas, selection, keyboard, and review UI.
- **Your own UI in the browser** — the framework-free cores. Same engine, no
  React: display lists, hit-testing, and a document handle.
- **A server, CLI, or Electron app in Node.js or Bun** — the `-native` packages. They
  call the native Rust facades directly and need no browser, canvas, or WASM runtime.
- **A server, a CLI, or a pipeline in Rust** — the three facade crates.
- **A server, a notebook, or an agent in Python** — the three `betteroffice-*`
  distributions on PyPI.
- **One stage of the pipeline** — the per-layer crates, published individually.

[Release notes](https://github.com/openooxml/betteroffice/releases) and each
package's changelog record shipped changes.

[Visual fidelity results](https://github.com/openooxml/betteroffice#visual-fidelity)
compare published packages and source builds with Microsoft Office on selected
corpus files. The [comparison harness](https://github.com/openooxml/betteroffice/tree/main/scripts/office-quality)
describes the measurement method.
