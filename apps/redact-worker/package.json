{
  "name": "@betteroffice/redact-worker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build:wasm": "bun scripts/build-wasm.ts",
    "predeploy": "bun run build:wasm",
    "deploy": "wrangler deploy",
    "dev": "bun run build:wasm && wrangler dev",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3",
    "wrangler": "^4.106.0"
  }
}
